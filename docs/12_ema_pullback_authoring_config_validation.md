# EMA Pullback authoring config validation

The Workbench instance shape is now an explicit Strategy Engine ingress contract. The endpoint translates it to the canonical strategy wire shape and executes the same feature-plan-backed semantic validation used by evaluations. Research Service does not carry a production copy of the strategy loader.
