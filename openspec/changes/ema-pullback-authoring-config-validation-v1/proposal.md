# Proposal
Add an authoritative Strategy Engine endpoint that validates current BBB/Workbench ema_pullback authoring instances and translates them to the canonical strategy envelope before semantic validation.
