# Requirements

The Strategy Engine SHALL expose a versioned per-bar decision contract sufficient for a separate Research Service to execute fills without importing strategy internals.

The Strategy Engine SHALL NOT return executed fills, completed trades, fees or PnL.
