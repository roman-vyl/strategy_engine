# Patch notes

Current sandbox includes completed OpenSpec Slice 1 and Slice 2.

Latest increment exposes `POST /v1/strategy-evaluations/live-entry` with strict request/response models, stable typed errors, OpenAPI schemas, and HTTP/application edge-case coverage.
